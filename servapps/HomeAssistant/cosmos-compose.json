{ 
  "cosmos-installer": {
    "form": [
      {
        "name": "adminOnly",
        "label": "Do you want to make this service admin only?",
        "initialValue": false,
        "type": "checkbox"
      }
    ],
    "translation": {
      "de": {
        "form.adminOnly.label": "Möchten Sie diesen Dienst nur für Administratoren zugänglich machen?"
      },
      "fr": {
        "form.adminOnly.label": "Souhaitez-vous rendre ce service accessible uniquement aux administrateurs ?"
      },
      "it": {
        "form.adminOnly.label": "Desideri rendere questo servizio accessibile solo agli amministratori?"
      }
    }
  },
  "minVersion": "0.22.30",
  "services": {
    "{ServiceName}": {
      "image": "lscr.io/linuxserver/homeassistant:latest",
      "container_name": "{ServiceName}",
      "restart": "unless-stopped",
      "security_opt": [
        "no-new-privileges:true"
      ],
      "environment": [
        "PUID=1000",
        "PGID=1000",
        "TZ=auto",
        "PROXY_HOSTNAME=['0.0.0.0/0','::/0']",
        "DOCKER_MODS=linuxserver/mods:homeassistant-hacs|aseracorp/docker-mods:homeassistant-trustedproxy|aseracorp/docker-mods:homeassistant-oidc",
        "OIDC_CLIENT_ID=__route_{ServiceName}",
        "OIDC_CLIENT_NAME=Cosmos-Login",
        "OIDC_DISCOVERY_URL={RootProtocol}://{RootHostname}/.well-known/openid-configuration",
        "OIDC_VALIDATE_TLS=false",
        "OIDC_LINK_USER=true",
        "OIDC_CREATE_USER=true",
        "OIDC_BLOCK_LOGIN=false",
        "OIDC_USERNAME_FIELD=sub",
        "OIDC_DISPLAYNAME_FIELD=sub",
        "OIDC_GROUPS_FIELD=roles",
        "OIDC_GROUPS_SCOPE=roles",
        "OIDC_ADMIN_NAME=admin"
      ],
      "labels": {
        "cosmos-force-network-secured": "true",
        "cosmos-auto-update": "true",
        "cosmos-icon": "https://azukaar.github.io/cosmos-servapps-official/servapps/HomeAssistant/icon.png",
        "cosmos-stack": "{ServiceName}",
        "cosmos-stack-main": "{ServiceName}"
      },
      "volumes": [
        {
          "source": "{ServiceName}-config",
          "target": "/config",
          "type": "volume"
        }
      ],
      "routes": [
        {
          "name": "{ServiceName}",
          "description": "Expose {ServiceName} to the web",
          "useHost": true,
          "target": "http://{ServiceName}:8123",
          "mode": "SERVAPP",
          "Timeout": 14400000,
          "ThrottlePerMinute": 12000,
          "BlockCommonBots": true,
          "SmartShield": {
            "Enabled": true
          },
          "AuthEnabled": false,
          {if Context.adminOnly}
          "AuthAdmin": true,
          {/if}
          "PublicOpenIDName": "__route_{ServiceName}",
          "PublicOpenIDRedirectURIs": "{RootProtocol}://{Hostnames.{StaticServiceName}.{StaticServiceName}.host}/auth/oidc/callback"
        }
      ]
    }
  }
}